// Generated by CoffeeScript 1.7.1
(function() {
  var binding;

  if (process.platform === 'linux' && process.type === 'renderer') {
    module.exports = require('remote').require('clipboard');
  } else {
    binding = process.atomBinding('clipboard');
    module.exports = {
      has: function(format, type) {
        if (type == null) {
          type = 'standard';
        }
        return binding._has(format, type);
      },
      read: function(format, type) {
        if (type == null) {
          type = 'standard';
        }
        return binding._read(format, type);
      },
      readText: function(type) {
        if (type == null) {
          type = 'standard';
        }
        return binding._readText(type);
      },
      writeText: function(text, type) {
        if (type == null) {
          type = 'standard';
        }
        return binding._writeText(text, type);
      },
      clear: function(type) {
        if (type == null) {
          type = 'standard';
        }
        return binding._clear(type);
      }
    };
  }

}).call(this);
